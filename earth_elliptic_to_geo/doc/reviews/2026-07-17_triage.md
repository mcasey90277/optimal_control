# Core review 2026-07-17 — three-way triage

Reviewers: **GPT-5.6-terra** + **Gemini 3.1 Pro** (crush/OpenRouter, inlined source,
no `-c` for the GPT slot per the known hang) + **host Claude** (adjudication below).
Scope: the four questions in `2026-07-17_review_prompt.txt` — dual anomaly (Q1),
5 N min-time stall (Q2), Cartesian baseline integrity (Q3), MEE go-forward plan (Q4).
Source under review: `kepler_lt_params.m`, `lt2b_rhs_time.m`, `casadi_lt_2body.m`,
`verify_pmp_2body.m`, `run_mintime.m`.

**Headline: the host's leading hypothesis was REFUTED by both reviewers independently,
on algebra, before any experiment was built. That alone justified the review.**

---

## Q1 — dual/primer anomaly

### Both agree (high confidence — two independent derivations, same result)

Correct α-stationarity for this 9-state cScale-augmented Sundman trapezoid:

    alpha_k  ∥  ±( h_{k-1}·Λ_v,k-1 + h_k·Λ_v,k )        [terra, general mesh]
    alpha_k  ∝    ( Λ_v,k-1 + Λ_v,k )                    [gemini, uniform mesh]

- The interval→node indexing in `verify_pmp_2body.m` **is** wrong (interval k's dual
  assigned to node k's control) — but on our **uniform τ-mesh** the correction is
  exactly the simple adjacent average = **our refutation experiment (b)**, which moved
  17.26°→17.22°. Terra states it plainly: *"this indexing error alone does not explain
  17–24°."*
- **cScale CANNOT rotate the primer.** Its defect row is `c_{k+1} − c_k`, whose
  derivative w.r.t. α and v is **zero**; and the factor `q_k = c_k·κ_k` multiplying the
  dynamics is a **common node-local scalar that cancels out of the direction**
  (both reviewers, independently). Gemini: *"neither κ, cScale, nor dτ shift the direction."*
- Therefore the residual anomaly lives in the **dual EXTRACTION**, not the map.

**⇒ HOST HYPOTHESIS "cScale slack-state coupling rotates the primer" is DEAD (as algebra).**
`DESIGN_dual_map.md` §2 H1 must be rewritten; its Step 2 (8-state A/B) keeps its value
but for a *different* reason (below).

### They disagree on mechanism (this is the productive split)

| | terra | gemini |
|---|---|---|
| mechanism | extracted `opti.dual` is not the convention/order the diagnostic assumes — **or the duals are not a usable KKT certificate at all** | IPOPT `nlp_scaling_method` **row-scaling** of the cScale-coupled KKT, imperfectly un-scaled ⇒ retrieved λ_v is a "rotated projection" |
| decisive test | full Lagrangian gradient; check the **tangential** residual `(I − αα')·∂L/∂α_k` (tangential because the cone multiplier absorbs the radial part) | the **8-state elimination** re-solve (absorb c* into the clock, drop row 9) |

**Host adjudication.** The two are compatible: Gemini names a *specific* corruption
channel; terra names the *test that discriminates corruption from a broken map*. Note
cScale survives as a suspect — **not as algebra, but as the source of scaling cross-talk**.
Gemini's mechanism implies a test neither named explicitly and cheaper than both:

> **T0 (new, cheapest): set `nlp_scaling_method='none'`, re-solve at the solution,
> re-check the primer.** One option line. If Gemini is right this moves immediately.

**Revised Campaign B order:** T0 (scaling off) → T1 (terra's tangential residual —
tells us whether the duals are a KKT certificate at all) → T2 (8-state elimination —
now a *formulation-equivalence / scaling-isolation* test, not a coupling test).
Terra's caveat retained: the 8-state test *"does not make a single-interval map correct"* —
the averaging fix is required regardless.

### Findings to fix regardless of mechanism

- **[CORRECTNESS]** `verify_pmp_2body.m:34-43` (+ `casadi_lt_2body.m:162-166`) — use the
  step-weighted adjacent-interval average for the nodal primer, one-sided at endpoints.
  (Necessary but NOT sufficient — proven by experiment (b).)

---

## Q2 — 5 N min-time stall

### Agreement

- Both endorse the host's **warmTight-on-infeasible-iterate** diagnosis as *at minimum*
  a real policy bug, and both say **test it first** (terra: *"cheapest and most
  discriminating"*; gemini ranks it #1: *"exactly right"*).
- Both independently flag a defect the host missed: **the free-longitude manifold does
  not enforce prograde** — it admits the retrograde GEO circle as a legitimate branch
  (terra #4, gemini #2). Convergent finding from two blind reviewers ⇒ high confidence.
- Both: the rejected time-stretch seed is indeed topology-flawed (stretching adds
  duration, not revolutions).

### Disagreement on ranking

terra ranks **winding-basin at doubled rev count** #1 and warmTight #2; gemini ranks
warmTight #1. **Host adjudication:** run the warmTight test first (both agree it is
cheapest), but do **not** expect it alone to cure a basin problem (terra's warning).
Note the cold tangential seed *does* wind the correct rev count by construction, which
weakens the pure-basin story for the **cold-seed** path — but not for the continuation
rounds, where each re-solve can drift basins.

### Findings

- **[ROBUSTNESS]** `run_mintime.m` (continuation calls) — barrier/warm-start policy is
  selected by *call number*, not by *feasibility*. Fix: `warmTight=false`
  (adaptive μ) while `maxDefect ≥ 1e-6`; tight only below; never reuse multipliers from
  a restoration/failed solve; retain a new primal iterate only if feasibility improved.
- **[ROBUSTNESS]** `casadi_lt_2body.m:103-109` — manifold terminal lacks prograde
  angular-momentum condition. Fix: add `h_z(end) ≥ h_min > 0` (or a branch homotopy).
  DESIGN.md §2 already *noted* the retrograde branch exists and deferred a guard
  ("add only if the solver ever drifts") — two reviewers now say add it.

---

## Q3 — baseline integrity: CLEAN (both)

- terra: *"Nothing shown invalidates the reported 10 N Cartesian baseline."* Dynamics
  MX-safe; Sundman/cScale structure correct; objective measure `dt = c·κ·dτ` correctly
  trapezoid-integrated; BCs correct; units correct; dV/mass bookkeeping correct.
- gemini: *"The Cartesian baseline is highly solid."*
- **1376.74 kg stands as the MEE validation gate.**
- **[ROBUSTNESS]** `casadi_lt_2body.m:147-152` — the post-solve numeric defect re-check
  uses `norm(r)^pSund` while the NLP uses the softened `(r'r + 1e-12)^(pSund/2)`. Negligible
  at our radii, but a verifier should certify the *actual* NLP. Fix: use the identical
  softened expression.
- terra's caution adopted: **do not gate the MEE successor on the raw-dual primer metric
  until Q1 is resolved** — i.e. Campaign B before/parallel to Campaign A Phase 2.

---

## Q4 — MEE + L go-forward

### Agreement

- L-as-independent-variable is sound; **L̇ > 0 strictly** for this regime (thrust ~1e-4 g
  cannot stall h/r²) — but log/enforce a margin.
- **L subsumes the Sundman clock** — no separate τ or cScale needed. (Gemini adds the
  elegant point: dividing by L̇ automatically concentrates mesh at apogee, exactly where
  the burns are.)
- Equinoctial is correct for e→0, i→0; the only true singularity (i=180°) is far away.
- Both endorse the plan.

### Disagreement 1 — node floor

terra **25–40/rev** ("15 is aggressive, not a safe production number"; ~22.6k intervals
at 754 revs, demonstrate convergence at 20/30/40) vs gemini **12–16/rev** absolute floor
(~9–12k points). Host estimate was 15–25.
**Host adjudication:** plan for **25/rev nominal with a 15/rev floor probe**, and settle
it empirically with the convergence study terra prescribes (20/30/40). Both agree the
answer is O(10–20k) nodes ⇒ **tractable**, which is the decision-relevant fact.

### Disagreement 2 — does MEE+L defeat the paper's objection?

- terra: *"substantially weakens, but does not completely defeat"* — remaining challenge
  is locating ~1500 switches and holding the KKT basin over 754 revs.
- gemini: *"The Paper is Outdated ... Your MEE + L plan fundamentally defeats their
  argument"* — the 2004 objection assumed dense-Jacobian SQP + finite differencing;
  a ~1e5-variable **sparse** NLP with exact AD Hessians into IPOPT/MUMPS is a different
  machine.

**Host adjudication: they are arguing about different axes, and both are right.**
- The **variable-count** objection is defeated (gemini's historical point is apt; our own
  campaign routinely solves ~1e4–1e5-variable sparse NLPs).
- The **switch-location / basin** objection is untouched by coordinates (terra's point;
  and gemini's own (e) concedes it — see below).
This is the honest claim for the paper: *"MEE+L defeats the dimensional objection; the
residual difficulty is combinatorial (switch structure), not dimensional."*

### Disagreement 3 — how to handle trapezoid switch-smearing

- terra: **hp/adaptive mesh refinement around switching-function zeros**, or a multi-phase
  formulation with **switch times as decision variables**. HS improves smooth arcs but
  does not remove the discontinuity floor; global PS is *worse* unless segmented at switches.
- gemini: agrees HS/PS don't help ("Gibbs ringing", "still smears"), but prescribes
  **stopping the ε-continuation at ε≈1e-4** instead of 0 — absorbing smearing into
  physical fuel rather than KKT violation.

**Host adjudication: terra's is better science; gemini's is a fallback.** Stopping at
ε>0 biases m_f — and our own tulip campaign explicitly *fixed* a legacy schedule that
stopped at 1e-3 precisely because it left an objective bias. **Decisive:** we already
own terra's prescription — `PSR/` (PMP-Steered Refinement) is exactly switching-function-
steered mesh refinement, already built and validated on the tulip problem. **Campaign A
Phase 2 should port PSR to the MEE solver** rather than accept an ε-bias.

### NEW — gemini's structural insight (neither host nor terra had it)

**ΔL (total longitude span) as a scalar decision variable**, with the mesh on a fixed
unit grid σ∈[0,1], L(σ)=L₀+σ·ΔL, and `dt/dσ = ΔL/L̇`. Consequence:

> **The revolution count becomes a DECISION VARIABLE.** This is precisely what
> Cartesian+Sundman could not do — there the rev count is frozen into the seed's
> geometry, which is *why* the time-stretch thrust-continuation was topology-flawed
> (stretching adds duration, not revolutions). In MEE with ΔL free, thrust continuation
> 10 → 5 → 2.5 → … → 0.1 N works because **the optimizer grows ΔL itself**.

Gemini's (e) then makes thrust continuation *mandatory*: a cold 0.1 N solve will trap
against 699/701-rev neighbours; only geometric Tmax-continuation reaches the 754-rev basin
safely. **This reframes Campaign A**: thrust continuation returns as the backbone — the
mechanism the host abandoned for good reason in Cartesian becomes correct and necessary
in MEE. It is also exactly the paper's own "discrete continuation" device.

- **[ROBUSTNESS]** (gemini) at ΔL ≈ 4700 rad, `cos(L)`/`sin(L)` on raw large operands
  risks cancellation/derivative damage — wrap to `mod(L,2π)` inside the dynamics or
  ensure analytic AD derivatives on the trig.

---

## Action list (ranked)

**Campaign B (`DESIGN_dual_map.md`) — rewrite §2/§3:**
1. Kill H1 (cScale coupling) as *algebra*; re-cast as **scaling cross-talk** (gemini) vs
   **extraction convention** (terra).
2. New step order: **T0** `nlp_scaling_method='none'` re-solve → **T1** tangential
   Lagrangian residual `(I−αα')∂L/∂α` → **T2** 8-state elimination (equivalence test).
3. Apply the weighted adjacent-average map regardless (**[CORRECTNESS]**, necessary-not-sufficient).

**Campaign A (`DESIGN_thrust_ladder.md`) — amend:**
4. Phase 0: keep μ-staged fix as first test (both endorse), but adopt terra's framing —
   feasibility-selected policy, no multiplier reuse from restoration, keep-if-improved.
5. Add **prograde guard** `h_z(end) ≥ h_min > 0` to the manifold (**both reviewers**).
6. Phase 2 rewrite: **ΔL as decision variable** + **Tmax geometric continuation as the
   backbone** (gemini) + **port PSR** for switch-aware refinement (terra) + `mod(L,2π)`
   trig guard; node budget **25/rev nominal, 15/rev floor probe**, convergence study at
   20/30/40.
7. Honest claim update: MEE+L defeats the *dimensional* objection; the *combinatorial*
   (switch/basin) difficulty remains — thrust continuation is the answer to it.

**Housekeeping:**
8. **[ROBUSTNESS]** post-solve defect re-check should use the softened radius (certify
   the actual NLP).
9. Do not gate MEE validation on the raw-dual primer until Campaign B closes.
