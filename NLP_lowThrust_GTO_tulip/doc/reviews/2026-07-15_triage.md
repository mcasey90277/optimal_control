# Core-numerics external review (2026-07-15) — triage

Reviewers: GPT-5.6-terra + Gemini 3.1 Pro (opencode-headless), host Claude.
Scope: the 3 CasADi solvers (casadi_energy_freetf, casadi_mintime_freetf,
casadi_minfuel_sundman) + the homotopy/seed recipe (sundman_homotopy,
gen_elfo_energy_gravhom).

## Verdict: core is CORRECT. Both reviewers confirmed the physics/collocation.
No result-changing bug. GP9 (gravhom:141 "syntax error") is a FALSE POSITIVE
(it's inside a `%#ok<NASGU>` comment; checkcode clean).

## Claim-precision (fold into the method note; sharpen docstrings)
- GP1: Sundman reduces the singular Hessian from ~r1^-4 to r1^(p-4)=r1^-2.5 at
  p=1.5 -- TAMES but does NOT eliminate. casadi_minfuel_sundman docstring says
  "bounded/mild" -> overstated; correct to "reduced by r1^p, residual r1^(p-4)."
  (This is exactly why the exact Hessian still hurts near the Moon.)
- GP2: casadi_mintime_freetf solves the ALL-BURN-restricted min-time (s==1);
  all-burn is optimal here (primer-verified) but label it as the restriction.
- GP3/G1: the gravhom ladder + fixed-tf solver PIN t_f (tfTarget); cScale absorbs
  the clock change. "free-tf" = the banded machinery, NOT a free-time solve. The
  cScale slack's benefit is decoupling trajectory shape from the tf/tauf ratio.
- GP6: primer-alignment/transversality are scale-invariant INDICATORS from the
  KKT duals (discrete adjoint up to +scale/global sign), NOT a full switching-law
  certificate; verify_direct_pmp (adjoint-ODE) is the stronger check. Present §7
  honestly.

## Robustness/efficiency (real, non-blocking -> future code-hardening pass / (f))
- G2/G4: step_solve returns FAIL if the tight re-clean fails after a loose-probe
  success, without trying the tight-from-Xk fallback. Cascade the fallbacks.
- G3: velocity bounds [-12,12] are close to GTO perigee speed ~9.9 ND; widen to
  [-25,25] (pos to [-5,5]) so bounds only block genuine divergence.
- GP4: no primary-exclusion constraint; add dd'dd>=r1Min^2, rr'rr>=r2Min^2 above
  the 1e-12 softening scale.
- GP5: API allows tfTarget=[] with tfWeight=0 (ill-posed free-time energy);
  guard/reject that combination.
- GP7: unit-alpha leaves a flat direction at coasts (s=0); campaign chose it over
  the (w,s) cone deliberately (cone wedges worse). SOCP norm(w)<=s is an alt.
- GP8: use limited-memory Hessian during continuation, exact to finish (our
  exact-Hessian-hurts lesson).

## Cosmetic
- GP9 false positive (comment, not syntax). Stale %#ok<NASGU> notices (Task 4).
