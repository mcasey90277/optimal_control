# cr3bp_common — TODO

- [ ] Nothing active. Stable shared problem definition for GTO_tulip /
  GTO_ELFO. Future: if a third CR3BP GTO campaign appears, revisit whether
  `minfuel_config` should split campaign config from constants.
- [ ] Relationship to `costate_common` (which serves the catalog
  campaigns): deliberate separation — this folder is the GTO min-fuel
  problem definition, that one is the family-agnostic min-time pipeline.
  Merge only if a real consumer needs both (none does today).
