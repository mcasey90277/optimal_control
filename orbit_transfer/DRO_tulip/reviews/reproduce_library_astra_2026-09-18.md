**NO-GO as an unattended, self-certifying rebuild.** The basic script-to-driver wiring is correct. The substantial risks are incomplete input validation, non-transactional resume/final publication, and acceptance checks that can either reject legitimate reproductions or accept insufficient evidence.

The MAT-file contents and several full callees are not supplied. Where a conclusion depends on them, I identify that explicitly rather than infer certification from filenames.

## What checks out

- **All fields constructed in `reproduce_library_70mN.m:102–124` are accepted by `run_phase_torus.m:316–343`.** `librarySeeds` really enables the shipped-library seeds; `discover=false` disables discovery, **not** filler registration or promotion.
- The five stated arrival phases are admissible. They need not all be grid points: `fast2` at `0.8671` is slightly off the 24-point lattice, but rescanning supports that. Names such as `cell11` need not equal indices in the sorted grid. **Whether each file actually contains the corresponding root at departure phase zero cannot be established from the supplied text.**
- Ten successfully adopted destination files make `need` empty (`run_phase_torus.m:173–189`). No `.done` files are required for these adopted files. Their names match `arrival_arc_70mN_*.mat`; the sheet rescans their paths at the requested phases (`build_arrival_sheet.m:55–76`).
- **The default run is not guaranteed to take three rounds or end in `budget`.** It ends in `done` as soon as a round registers no new spine root and adds no anchor—even if off-spine filler work occurred or holes remain. If round three still registers roots/adds anchors, it ends in `budget`. Normal completion publishes `final` in either case (`run_phase_torus.m:265–305`).
- The shell quoting helpers correctly implement MATLAB literals and POSIX single-quoted arguments. In the generated wrapper, `echo $!` does **not** change `$!`; `wait $!` waits for the background command just launched. With the normal foreground `matlab -batch` launcher, the wrapper cannot race MATLAB’s still-running `.done` flush: it checks after that process exits. Detached launchers or duplicate writers are different cases.
- `roundExtras` produces a row cell array of paths. `unique([extra, {fh.file}], 'stable')` is valid MATLAB and preserves first-occurrence order. The surrounding braces in the `struct` constructor correctly store that cell array in a scalar struct.
- The closures-only changes in `build_ribs` and `fill_holes_direct`, and the first-anchor forwarding through `commonOpts`, repair the stated operating-point problems.
- **Greedy partition pairing does not invalidate the equality test for valid, complete labels.** Identical partitions have exactly one positive overlap per corresponding row/column, so all cells match. Conversely, matching every cell through a one-to-one pairing implies identical partitions. Greediness affects the *number/location* of differences for unequal partitions, not this zero-difference equivalence.

## Campaign inputs, resume, and publication

- **[CORRECTNESS]** `reproduce_library_70mN.m:118–145,163–167`; `arclength_arrival.m:120–160` -- **The launch preflight verifies filenames and a registry count, not the required anchor/registry contracts.** The plan output proves neither the anchors’ departure phases nor their certification. With adopted arcs, only the first anchor is passed through setup; the other four can remain unchecked. The registry is optional in the implementation despite being part of the stated reproduction recipe. Its reader accepts `direct` entries containing the required fields, but the driver’s append at `run_phase_torus.m:842` requires matching struct fields; an extra field such as `Y` can make appending fail with “dissimilar structures.” **Fix:** before copying or launching, require the seven-root registry, validate/normalize its schema to `sD,sA,tfDays,z,src`, and validate all five anchors against the specified problem and phases. Exercise each anchor’s setup at `sD=0`; do not treat setup’s convergence/time-movement guards as a replacement for certification.

- **[ROBUSTNESS]** `reproduce_library_70mN.m:127–165`; `run_phase_torus.m:133–160` -- **Resume identity excludes several inputs that determine the library, and adoption happens before the driver checks even its limited manifest.** Existing state wins over newly supplied anchors; changed `librarySeeds`, anchor contents, discovery policy, or adopted inputs can be silently ignored by a terminal campaign or mixed into an unfinished one. Conversely, a completed resume can fail during unnecessary re-adoption. `compareOnly` unnecessarily requires all anchor files and loads the seed registry before comparing. Also, `.adoptArcs=false` does **not** re-walk existing arcs, contrary to the printed promise. **Fix:** dispatch compare-only before build-input checks; validate the existing campaign before mutation; record hashes/provenance and build-defining options, while explicitly allowing operational changes such as a larger round budget. Require a fresh directory or explicit invalidation for re-walking.

- **[CORRECTNESS]** `run_phase_torus.m:272,279–305,151–155` -- **Terminal state is committed before `final` is published, and a resumed terminal campaign never repairs publication.** A crash after line 272, a failed required-file assertion, or interruption during `rmdir`/copy leaves a terminal state with an absent, partial, or older `final`. The next call returns “nothing to do” before reaching publication. **Fix:** make publication a persisted, resumable stage. Build a complete staged final directory, validate its receipt, then publish it and commit terminal completion. Terminal resumes must verify or repair the final generation, not merely return its intended path.

- **[CORRECTNESS]** `reproduce_library_70mN.m:183–205,234–243`; `run_phase_torus.m:285–286` -- **`REPRODUCED: PASS` can combine a catalog with an unrelated audit.** `lastAudit` searches round folders rather than identifying the audit of the exact catalog in `final`. An audit from an incomplete later round, an older successful attempt, or a subset audit can be selected. Testing only `nBad==0` accepts zero audited entries. The final publication does not include the audit. **Fix:** publish the catalog, audit, stage results, and receipt as one generation; bind the audit to the catalog hash and policy version. Require the audited entry set to equal the catalog’s complete entry set, with 576 successes for this campaign. Report campaign completion separately from catalog equality; do not disguise `budget` as a completed fixed point.

- **[CORRECTNESS]** `run_phase_torus.m:238–272,636–644,736–746`; `run_costate_library.m:@@ -235,12 +248,26 @@` -- **The fixed-point decision uses this invocation’s counters, not progress during the unfinished round.** For example, a root is durably registered, then the driver dies before round bookkeeping. On resume it is a duplicate, so `nRegistered` is zero. The existing round sheet is reused because it already exists, even though that sheet may predate the new registry/anchors. The resumed round can declare `done` without ever incorporating that progress into a new sheet. The holes-file repair does not resolve this. **Fix:** persist a round-start input fingerprint and a round progress journal. A fixed point requires a completed round using the current registry/anchor generation, with no changes since that round’s input snapshot. Invalidate cached sheets when their dependencies change.

- **[CORRECTNESS]** `run_phase_torus.m:636–643,652–673,736–746` -- **“Already registered” does not mean “already judged for promotion.”** Both paths save registration before promotion, so interruption between those operations permanently suppresses promotion. Discovery also has a non-crash case: seed one registers a root; seed two returns the same root with a slightly smaller time within the duplicate tolerance; `bestAdded` becomes false when `best` is replaced. That root is then never promoted. **Fix:** track promotion disposition independently of registration, with a durable pending state. Associate “new/pending” with root identity, not the latest solver call’s `added` flag; complete pending decisions idempotently on resume.

- **[CORRECTNESS]** `run_phase_torus.m:831–843` -- **Time-only deduplication can discard a genuinely distinct root.** Same arrival phase and flight time within `1e-3` day—86.4 seconds—is not root identity, particularly given the explicitly acknowledged near-ties between families. Departure phase is also ignored when comparing against an adopted registry. **Fix:** include departure phase and a validated normal-chart costate/root-distance test. Use flight time only as a preliminary filter, not the identity predicate.

## Process ownership and P0 orchestration repairs

- **[ROBUSTNESS]** `run_phase_torus.m:469–514` -- **PID existence is mistaken for job ownership, and legacy state files are not migrated.** `kill -0` establishes process existence/permission, not identity; reused wrapper or MATLAB PIDs can cause a six-hour false adoption followed by killing an unrelated process. Legacy `.jobs.file` values naming job scripts never match `need(:,5)`, and `<job-script>.pid` is not the arc’s PID file: live jobs are dropped and duplicate writers can be spawned. **Fix:** version/migrate job records; store both job and arc paths, a job UUID, and process start identity. Verify ownership before adoption or signaling. Refuse ambiguous legacy live jobs rather than forgetting them. Validate positive integer PIDs and supervise the owned process group through confirmed termination.

- **[ROBUSTNESS]** `run_phase_torus.m:440–446,535–537,582–584` -- **There is still an unrecorded-launch window.** All jobs are launched before the updated state is saved. A driver crash—or a later spawn failure—can leave live earlier jobs absent from `st.jobs`. Their PID files are not independently reconciled on resume; spawning again removes their markers and creates competing writers. **Fix:** reserve each output under a campaign/job lock before launch, persist a launch record immediately, and reconcile durable job manifests/PID identities before any retry. Persisting after each launch narrows, but does not eliminate, the spawn-to-save window.

- **[ROBUSTNESS]** `run_phase_torus.m:601–617,860–869` -- **Supervisor adoption can wait 48 hours for a verdict that already exists.** If the supervisor has just published its verdict but has not exited, `before` records that verdict’s timestamp, the supervisor is adopted, and the waiter insists on a *newer* verdict that will never arrive. Timestamp comparison also does not identify which attempt produced a verdict. On deadline, this path returns without stopping the supervisor/workers. **Fix:** use attempt IDs and atomic verdict publication; accept the matching attempt’s existing terminal verdict. Monitor owned-process liveness and clean up owned jobs on timeout.

- **[ROBUSTNESS]** `run_phase_torus.m:799–810`; `run_costate_library.m:@@ -538,11 +565,39 @@, @@ -668,7 +723,9 @@` -- **Stage checks reject explicit failure but do not positively prove that required stages ran.** Ignoring `NaN` is consistent with “check stages that ran,” and the two generated finalizer lines correctly add that check. It is insufficient for these calls, which explicitly require package, audit, and sweep. Moreover, `stageOutcomes` infers execution from switches and blocker prefixes: a packaging-chain exception can leave audit/sweep unexecuted yet label them true. The package failure currently prevents acceptance in that example, but the stage report is not reliable evidence. **Fix:** record outcomes at actual stage execution/completion. For these callers, require all three named outcomes to be true; missing or `NaN` is failure to establish completion.

## Certification and audit changes

- **[CORRECTNESS]** `certify_root.m:@@ -379,6 +384,22 @@`; `build_arrival_sheet.m:115–119` -- **The new certificate fields need a uniform schema on every return path.** The shown diff assigns `C.minLamVBound` and `C.minQmtBound` only after their individual gates pass. Unless the omitted certificate initializer already initializes both fields, early failures, H3 failures, and successes return different field sets. MATLAB struct-array assignment requires matching field names; the sheet appends both successful and failed certificates directly. This can abort the sheet build on its first mixed outcome. **Fix:** initialize both fields to `NaN` when constructing every `C`, and test mixed success/early-rejection arrays. This is a launch-blocking verification item because the full initializer was not supplied.

- **[CORRECTNESS]** `mintime_hypothesis_gates.m:@@ -116,6 +120,16 @@`; `between_sample_bound.m:18–23,46–52` -- **The new “whole-arc lower bounds” are explicitly estimates, not guaranteed lower bounds.** The formula is valid given an interval-wide derivative bound. Taking the maximum of derivative-bound values at the interval endpoints does not establish such a bound inside the interval; the helper’s own documentation acknowledges this. An interior spike can invalidate the claimed lower bound and permit a false H2/H3 pass. Conversely, an unnecessarily loose slope estimate can reject a positive, well-behaved trajectory. **Fix:** obtain interval-wide enclosures, or explicitly classify these quantities as sampled numerical evidence rather than a certified whole-arc exclusion. Adaptive refinement and convergence checks improve numerical confidence but are not themselves validated enclosures.

- **[CORRECTNESS]** `audit_phase_catalog.m:@@ -132,21 +144,44 @@` -- **The audit’s new gate validators are weaker than the certifier’s validators.** Scalar `Inf` passes the H2/H3 `> hypFloor` checks. `logical(2)`, `logical(-1)`, and `logical(Inf)` are true, so malformed H6 values pass. MATLAB rejects conversion of `NaN` to logical, causing an exception rather than the promised BAD row. Struct arrays and malformed nonvector polish outputs can likewise escape the intended row-level handling. **Fix:** require scalar structs and real, finite scalar measurements using the same validation helper as the certifier; require H6 to be an accepted Boolean representation, specifically 0 or 1. Validate vector shape and catch malformed instrument output per row.

- **[ROBUSTNESS]** `audit_phase_catalog.m:@@ -57,6 +63,12 @@, @@ -132,21 +144,44 @@`; `certify_root.m:@@ -379,6 +384,22 @@` -- **The new audit is a stricter acceptance policy than the record’s audit, so “previously certified” does not guarantee this rebuild can finish.** A legitimate old entry can now be BAD because a call times out, a conservative between-sample estimate falls below `1e-5`, rank/H6 decisions change under different numerical settings, or re-polishing moves more than an absolute `1e-6`. Small shooting residual does not imply small root-coordinate movement in an ill-conditioned problem. **Aborting publication on unestablished certification is correct; reverting to fail-open is not.** **Fix:** run the new audit against the record before committing the rebuild budget; preserve the exact certification policy/options. Distinguish numerical/infrastructure inconclusiveness from demonstrated invalidity, permit bounded retries/refinement, and checkpoint successful checks. Do not silently relax the gates merely to reproduce the old count.

The dense-scan multiplicity addition prevents the specified inconsistent “clear” result. The override change also correctly prevents an instrument-overridden certificate from shipping, assuming `C.ok` is initialized false as the existing design requires.

## Comparator

- **[CORRECTNESS]** `compare_phase_catalogs.m:73–80,99–104` -- **Nonfinite solutions can compare equal.** For example, a covered cell containing `NaN` in time or costates produces a `NaN` difference; MATLAB evaluates `NaN > tolerance` as false. Neither over-tolerance counter increases. **Fix:** validate real, finite, positive times, finite eight-component roots, valid entry indices, and agreement between stored `tf_nd` and `z8(8)` before comparison. Nonfinite computed differences must be errors or explicit differences, never passes. Validate tolerances as finite nonnegative scalars too.

- **[CORRECTNESS]** `compare_phase_catalogs.m:60–68,84–87,109–118` -- **Missing family information and mismatched problem identities can still report PASS.** If either `family_index` field is absent, the family comparison is silently disabled while the report says zero differences. Only phase coordinates are checked for problem identity; the reference time scale is discarded. Identical arrays attached to different engines, orbit branches, or dimensional scales can therefore pass. **Fix:** require complete, meaningful family assignments or report “not established,” and validate the full problem identity and supported catalog dimensions/rungs before comparison. Either assert equal time scales or convert each catalog using its own verified scale.

- **[CORRECTNESS]** `compare_phase_catalogs.m:76–78`; `audit_phase_catalog.m:@@ -132,21 +144,44 @@` -- **The root metric can mask costate disagreement and is inconsistent with the audit’s absolute movement limit.** Including `t_f` in the denominator allows a large time component to dilute changes in small costates; large costate components can similarly hide changes in smaller ones. In the normal chart there is no arbitrary common costate scaling to quotient out. The stated metric is mathematically legitimate, but it is not automatically a strong same-root criterion. `1e-6` day is 0.0864 seconds; solver residual tolerances alone do not establish reproducibility at that time or coordinate tolerance. **Fix:** compare time separately and use documented costate scales with absolute-plus-relative tolerances, or a suitably weighted costate-only norm. Calibrate these through repeated same-root polishes, including poorly conditioned cases, and reconcile the audit movement test.

- **[ROBUSTNESS]** `compare_phase_catalogs.m:93–96`; `reproduce_library_70mN.m:202–203` -- **The reported number of agreeing cells is wrong, and table construction assumes row-oriented grids.** A cell can contribute both a numerical-difference row and a family-difference row; coverage rows also do not belong to `nBoth`. Therefore `nBoth-height(cells)` is not the count of agreeing cells and can be negative. For column phase vectors, `sr.sD_frac(ciD).'` produces a row rather than the table’s required column. **Fix:** compute agreement from a union of per-cell failure masks; store multiple reasons per unique cell. Construct coordinate columns with `reshape(...,[],1)`.

For unequal partitions, greedy pairing can overstate the minimum number of disagreements. For example, overlap matrix `[6 5; 5 0]` gives six paired cells greedily, versus ten under the optimal pairing. Use maximum-weight bipartite matching if the intended diagnostic is the *fewest* changed cells; this is not a zero-difference correctness defect.

## Arc adoption

- **[CORRECTNESS]** `adopt_walked_arcs.m:48–57`; `build_arrival_sheet.m:55–78`; `run_phase_torus.m:177–178` -- **Filename/byte equality is not arc identity or completion, and extra matching arcs are accepted.** A misnamed source is copied without checking `A`, `A.anc.sD`, anchor phase/root, or `A.B.problem`. Any additional `arrival_arc_70mN_*.mat` in an existing folder also enters the sheet. The driver considers any destination file sufficient, including one associated with a failed job. **Fix:** validate the ten expected arcs against an immutable adoption manifest, including problem identity, anchor identity, completion policy, and hashes. Reject or explicitly account for extra matching arcs. Distinguish externally validated adopted arcs from generated arcs whose successful verdict must be established.

- **[ROBUSTNESS]** `adopt_walked_arcs.m:44–58,65–73`; `reproduce_library_70mN.m:163–165` -- **Interrupted copying is fail-stop but not cleanly resumable.** An interrupted copy can leave a truncated destination; the next invocation refuses it as a conflicting arc. An interrupted registry copy is worse: existence suppresses re-adoption, then loading fails. A partially adopted folder containing only complete matching files otherwise resumes correctly; missing sources cause an error, not a silent family omission. `sameBytes` also loads both complete files simultaneously. **Fix:** preflight all sources, copy to unique temporary files, validate, then atomically publish. Recover recognized incomplete copies without overwriting genuine conflicting artifacts. Compare large files in chunks or by verified hashes.

## Launch decision

### **NO-GO as supplied**

Minimum changes/evidence required for GO:

1. **Validate and freeze the actual inputs:** all five anchor contracts, the seven-root registry schema, the ten arc identities, and absence of unapproved extra arcs.
2. **Resolve the certification blockers:** verify uniform certificate fields; harden audit scalar validation; establish what the H2/H3 “bounds” actually certify; successfully pre-audit the record under the new policy.
3. **Make resume/publication transactional:** persistent round progress, recoverable promotion decisions, and repairable final publication.
4. **Bind acceptance to the exact final generation:** complete audit, catalog hash, policy, stage completion, and 576 covered/audited cells.
5. **Close comparator false passes** for nonfinite data, missing family assignments, and mismatched problem identities.
6. **Before permitting any arc walking/resume:** migrate legacy jobs and establish durable process ownership. This path remains reachable through filler promotion even with discovery disabled.

### Ranked top five

1. **Unvalidated interval estimates are being consumed as whole-arc certification bounds.**
2. **PASS can rely on insufficient or mismatched evidence:** unrelated/partial audit, nonfinite comparisons, or omitted family checks.
3. **A terminal checkpoint can permanently strand an absent, partial, or stale `final`.**
4. **Interrupted-round progress and registration/promotion ordering can produce a false fixed point.**
5. **The stricter audit and potentially nonuniform certificate schema can abort the nominal rebuild after expensive work; neither compatibility is demonstrated by plan mode.**