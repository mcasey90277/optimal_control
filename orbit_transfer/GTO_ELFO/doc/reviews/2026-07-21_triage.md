# ELFO retarget-layer external review (2026-07-21) — triage

Reviewers: GPT-5.6-terra + Gemini 3.1 Pro (opencode-headless), host Claude
(adjudicator, with code spot-verification). Scope: the GTO->ELFO layer
(gravhom seeds -> tf-sweep -> energy->fuel sharpening -> export/verify +
min-time anchor), steered by the earth-campaign lessons L1-L6 and
thrust-ladder readiness. The 07-15 core review (which already covered
casadi_energy_freetf / casadi_mintime_freetf / gen_elfo_energy_gravhom
core numerics: CORRECT) was not re-litigated.

## Verdict

Nominal 25 mN trajectory physics TRUSTED (Gemini's "numerical precision"
doubt is REJECTED — see R2 in the tulip triage; defects are post-hoc
numeric re-checks). The published ELFO **factor labels/front coordinates
and exact switch counts are NOT trusted** until the anchor rebase (below)
and a mesh-band repeat. Not ladder-ready without the prep trio.

## CONFIRMED (host-verified where marked *)

- **C1* [CORRECTNESS, headline] Factors keyed to the TULIP anchor.**
  `minfuel_config.tfMin = 6.2906939607` (tulip) drives
  `tf = factor*cfg.tfMin` in `run_elfo_minfuel`/`elfo_run_one`/tf-sweep,
  the factor-keyed seed bank (`energy_elfo_f####.mat`), and the export
  `const.tfMin`. With the ELFO anchor certified 2026-07-15
  (tfMin_ELFO = 6.0961534862 ND), every "factor" label is ~3.2% off in
  ELFO terms (a documented convention that outlived its justification;
  physical t_f values are correct). **FIXED 2026-07-21**: drivers rebased
  on `cfg.tfMin_elfo`; seed selection now matches by PHYSICAL t_f (nearest
  within tolerance), making old tulip-keyed seed caches safely reusable;
  `elfo_collect_summary` re-derives factors from stored t_f, so old rows
  relabel correctly at read time. NOT rebased: `gen_elfo_energy_gravhom`'s
  `factor` — it selects the TULIP energy-backbone source seed (an input
  selector; legitimately tulip-keyed).
- **C2* [CORRECTNESS] Resume path can save two trajectories.**
  `gen_elfo_minfuel`: fully-resumed runs re-solve `finalInfo` UN-GATED and
  save `out=finalInfo` beside checkpoint `X,U`. **FIXED 2026-07-21**:
  `finalInfo` is now certification-gated and `X,U` are taken from it.
- **C3* [ROBUSTNESS] Gates admit `Solved_To_Acceptable_Level`** in
  gravhom/minfuel/tf-sweep step acceptance. **FIXED 2026-07-21**
  (`Solve_Succeeded` required), same as the tulip fix.
- **C4 [ROBUSTNESS] tf-sweep banks 500-iter loose solves without a tight
  re-clean** (unlike gravhom/fuel). OPEN -> TODO.
- **C5 [METHODOLOGY] Switch counts: exact integers, embedded in result
  FILENAMES; N inherited statically from the seed with no nodes/rev
  bookkeeping or mesh-band check.** OPEN -> TODO (mesh-band repeat; drop
  the integer from identity-bearing filenames at the next format change).
- **C6 [LADDER] No per-rung thrust provenance; checkpoints/seeds lack
  config fingerprints (resume checks tf only); fixed `cBox`, velocity
  boxes, no bound-saturation diagnostics; min-time driver saves even
  failed solves.** OPEN -> TODO (ladder-prep trio + anchor-save gate).

## REJECTED (do not act)

- **(Gemini) manual `N*D(:)==0` defect scaling + "precision untrustworthy"**
  — see tulip triage R2: refuted empirically; the fix is the L3 trap.
- **(Gemini) time-interpolated warm starts as the aliasing fix** — t-interp
  still aliases across rev counts; the correct pattern is steering-law
  regeneration (earth `warmstart_phase_beta`).

## Ladder-readiness (merged)

Breaks first at provenance (global thrust + fingerprint-less seed/checkpoint
reuse), then frozen spans (`cBox`, N), then acceptable-level dual creep
(fixed). Before the ladder: (1) per-rung thrust + fingerprints/namespaces;
(2) adaptive `cBox`/mesh + saturation diagnostics; (3) tf-sweep tight
re-clean + mesh-band study.

Raw reviews: `2026-07-21_elfo_review_{gpt56terra,gemini}.md`.
Fixes landed: see ../../TODO.md and the 2026-07-21 commits.
