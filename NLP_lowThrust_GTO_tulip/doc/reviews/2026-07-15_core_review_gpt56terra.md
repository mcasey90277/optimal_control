- **[CORRECTNESS]** `sundman_minfuel/casadi_minfuel_sundman.m:7-10` — The claim that \(p=1.5\) makes the perigee “gravity Hessian” mild/bounded is incorrect. The regularized acceleration behaves as \(r_1^{p-2}\); its position Jacobian as \(r_1^{p-3}\), and exact second derivatives as roughly \(r_1^{p-4}\). Thus \(p=1.5\) still has singular derivatives. Correct the claim; use collision avoidance and consider limited-memory Hessians during continuation.
- **[CORRECTNESS]** `elfo/casadi_mintime_freetf.m:76-77,112-113` — This solves **minimum time subject to always burning**, not general minimum time. A true minimum-time problem permits \(0\le s\le1\); PMP usually selects full thrust but can admit zero/singular arcs. Restore throttle and minimize `X(8,nN)`, or label this explicitly as the restricted all-burn problem.
- **[CORRECTNESS]** `elfo/gen_elfo_energy_gravhom.m:121-124,179-183` — Despite calling LEG 0 “free-t_f,” every ladder solve imposes `tfTarget=ctx.tf0`; \(t_f\) is pinned throughout. `cScale` only compensates for the changed clock. This is appropriate for well-posed min-energy, but it cannot provide the claimed reachability benefit of free final time. Correct the documentation/leg rationale, or remove `tfTarget` and add a physically scaled positive time penalty.
- **[CORRECTNESS]** `elfo/casadi_energy_freetf.m:128-130` — The \(10^{-12}\) denominator softening changes the CR3BP model near either primary, while no Earth/Moon exclusion constraint prevents the optimizer from entering that altered region. Add physically justified constraints such as `dd.'*dd >= r1Min^2` and `rr.'*rr >= r2Min^2`, with radii safely above the softening scale.
- **[ROBUSTNESS]** `elfo/casadi_energy_freetf.m:178-184` — The public API permits `tfTarget=[]` with `tfWeight=0`, an ill-posed free-time energy problem. Enforce `tfWeight>0` when `tfTarget` is empty, or reject that option combination.
- **[ROBUSTNESS]** `elfo/casadi_energy_freetf.m:239-253` — `lamDef` is not directly a continuous costate, and the primer comparison uses only one interval defect multiplier although nodal trapezoidal stationarity couples adjacent defects. The sign-flip heuristic also prevents certification of the correct sign. Report this as a heuristic only; reconstruct/scaled adjoints from the discrete KKT equations before claiming PMP agreement.
- **[ROBUSTNESS]** `sundman_minfuel/casadi_minfuel_sundman.m:110-111` — Enforcing unit `alpha` even at `s=0` leaves a direction manifold with no dynamical effect, degrading conditioning near coast arcs. Use a thrust-vector control with `norm(w)<=s` and acceleration proportional to `w`, or regularize/fix direction on coast nodes.
- **[EFFICIENCY]** `elfo/casadi_energy_freetf.m:188-216` — IPOPT is left on its exact Hessian near \(r_1\sim0.017\), where the regularized derivatives remain steep. For continuation, use `p.ipopt.hessian_approximation='limited-memory'`; optionally finish a converged seed with the exact Hessian.
- **[STYLE]** `elfo/gen_elfo_energy_gravhom.m:141` — `insertion = ...; provenance: ...` is executable MATLAB syntax, not a comment, and will error. Prefix `provenance:` with `%`.

The Sundman ODE scaling, two-primary clock asymptotes, physical-time homotopy measure, CR3BP signs, Moon-only gravity homotopy, and trapezoidal defect dimensions are otherwise correct.
