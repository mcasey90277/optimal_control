The visible PMP sign conventions are consistent with the **normal, regular, fixed-phase problem with free terminal mass and time**. The final verdict also correctly disclaims a rigorous certificate. However, the actual field, adjoint, STM, and diagnostic implementations are not supplied, so their correctness cannot be inferred from the reported fields alone.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:459-468` -- **A dense-scan FAIL need not block the successful verdict.** The script prints `pass(CS.clear)` but never requires `CS.clear` in `sufficient`. A result with `CS.clear == false` and zero values in the particular counters inspected at lines 466–467 leaves `s4Status == 'PASS'`. This is a visible enforcement gap, independent of how the helper currently constructs its result. **Fix:** require a validated `CS.clear` explicitly, reconcile it with the counters, and classify any inconsistent or incomplete result as unresolved. Print the combined S4 status **after** incorporating the dense scan.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:278-285` -- **The central study-script requirement is not implemented.** N2–N6 print aggregated results from `pmp_pointwise_checks`; S1–S3 and X2 similarly consume `mintime_hypothesis_gates` results at lines 364–381. Agreement with production instrumentation is useful, but it does not let the reader reproduce the argument or audit the coded Hamiltonian and adjoints. **Fix:** compute these quantities in the script or its displayed local functions, then optionally compare against the library. Explicitly show the OCP, including throttle domain and mass admissibility, and implement
  \[
  H=\rho+\lambda_r^\top v+\lambda_v^\top\!\left(g(r,v)+\beta T u/m\right)
       -\beta T\lambda_m/c,
  \]
  \[
  \dot\lambda_r=-g_r^\top\lambda_v,\qquad
  \dot\lambda_v=-\lambda_r-g_v^\top\lambda_v,\qquad
  \dot\lambda_m=\beta T\lambda_v^\top u/m^2.
  \]
  With \(g=\nabla\Omega+C v\), the velocity-adjoint Coriolis term is \(+C\lambda_v\). Under the minimizing direction, the mass-adjoint rate is negative. Show the resulting Hamiltonian, adjoint, control-gap, and throttle residuals with their own gates. The same principle applies to constructing and testing the lift matrix rather than merely displaying its reported rank.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:219-242` -- **This is a catalog-root restudy, not the requested direct-to-indirect study.** There is no Hermite–Simpson/Sundman OCP solve, no harvested duals, and no displayed multiplier-to-costate conversion. The starting point already comes from a “certified” indirect catalog. This does not invalidate a newly checked root, but it makes the teaching artifact circular at its most important transition. **Fix:** add the direct transcription, its feasibility checks, Sundman conversion, and dual normalization/sign conventions before building the multiple-shooting seed. Retain catalog loading only as an explicitly labeled accelerated mode. If the homogeneous chart is used, show the positive-\(\rho\) conversion to the normal chart rather than silently assuming it.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:436-454` -- **The free-time/free-mass Jacobi construction is absent, and its displayed identities have no script-level gates.** Neither the quotient nor its row/column selection is visible, so the reader cannot establish that the determinant belongs to this endpoint problem. For the declared 14-state ordering, the natural fixed-time bang-arc block is \(\Phi(1\!:\!6,8\!:\!13)\), after recognizing the zero mass row and the ineffective initial mass-costate direction. Removing the remaining right costate ray and left costate annihilator gives a \(5\times5\) quotient—but its equivalence to the required free-time/free-mass test must still be derived. **Fix:** display the variational equations, block selection, complement bases and their orientation, and the endpoint reduction. Explain the role of the free-time column and H6; in the normal chart its relevant clearance is \(1-(T/c)\lambda_m(0)\), in Hamiltonian units. Give scaled tolerances and actual PASS/FAIL gates for both kernel identities at line 454. Those numbers cannot remain decorative validity checks.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:436-464` -- **The scan does not establish “no conjugate time in \((0,t_f]\)” or close all sign-test blind spots.** The output explicitly admits uncovered start transients. Moreover, refining only candidates discovered by a finite coarse scan can miss a narrow pair of zeros or an even-order touch that generates no candidate. “No interior sign change” establishes only that the sampled determinant signs did not change; it does not establish nonsingularity between samples. **Fix:** label the result “no zero detected” unless stronger evidence is available. Supply a short-time asymptotic/desingularized Jacobi argument covering the excluded initial interval. For the remainder, demonstrate grid-shift, resolution, and integration-tolerance convergence and relate the singular-value floor to propagated numerical error. A certificate would require interval/rank bounds or another justified completeness argument, not just more samples.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:36-39` -- **The opening theorem claim is stronger than the argument actually supplied.** Lines 386–389 repeat that these checks give a strict strong local minimizer, while lines 528–531 explicitly acknowledge that subarc normality, the free-mass/free-time second-variation reduction, and existence of a nearby exact extremal remain unestablished. In particular, a one-dimensional stationary-lift space for the **whole arc with its terminal condition** does not, by itself, establish the required normality property on every relevant subarc. **Fix:** state the precise theorem and endpoint-manifold assumptions before the gate stack, identify which hypotheses are proved analytically and which receive only numerical evidence, and make the earlier claims conditional too. Failure of a sufficiency hypothesis is not, by itself, a refutation of minimality; the final fallback wording correctly avoids that inference.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:514-552` -- **Most failed checks do not make the script fail.** N2–N6, S1–S4, V1, and X1/X2 can fail, after which execution prints a verdict, plots, and terminates normally. They generally block the positive *textual claim*, but they do not provide the requested executable self-check. **Fix:** form a final scalar `studyOK` from every required gate and validity condition, preserve the diagnostics, and finish with a named assertion/error when it is false. Unresolved results should also produce a non-successful self-check outcome. Keep plotting available before that assertion if useful pedagogically.

- **[ROBUSTNESS]** `DRO_tulip/indirect/transfer_study.m:251-255` -- **The script does not expose or measure propagation accuracy relative to its gate tolerances.** The required tight `ode113` flight is hidden behind defaults. Lines 298–303 document a 180-fold change in residual between augmented and plain propagation, but a smaller residual is not evidence that one propagation is more accurate. Both quoted values happen to satisfy `tol.R`; neither establishes the claimed numerical uncertainty. Also, the arrival screens permit approximately \(2.57\times10^{-4}\) ND position error and \(9.83\times10^{-3}\) ND velocity error—much larger than `tol.R`. **Fix:** declare integration settings in section 0; repeat propagation and boundary-residual evaluation under refinement; report changes in arrival, Hamiltonian, and terminal mass costate. Require uncertainty comfortably below each threshold, especially `tol.lamm`. Recompute the multiple-shooting residual from the returned nodes rather than treating the solver’s scalar as an accuracy estimate. If the single-shot miss remains large, provide conditioning/nearby-root evidence before connecting the nodal root to an exact-endpoint extremal.

- **[ROBUSTNESS]** `DRO_tulip/indirect/transfer_study.m:64-66` -- **“One flight” is not what the second-order stack actually uses.** The hypothesis gates explicitly fly independently at lines 361–365; the junction conjugate data come from `it.conj`; the spectrum scan receives `z8` and propagates separately. Agreement of one scalar, `min Q`, at lines 497–499 cannot establish that these calculations followed sufficiently close trajectories, particularly near a sensitive lunar encounter. **Fix:** distinguish the authoritative flight from independent numerical realizations. Compare their states, costates, and controls on a common time grid, including agreement with the shooting junctions, and check all physics parameters and coverage explicitly. Either run the variational diagnostics along the authoritative dense flight or demonstrate that their trajectory discrepancy is negligible relative to the diagnostic margins.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:226-256` -- **The 1900 km lunar-clearance requirement has no visible calculation or explicit script policy.** `flight.admissibility.ok` may enforce the intended library default, but the supplied script neither names that threshold nor computes its margin. “Clear of both primaries” is not a reproducible substitute. **Fix:** put the clearance thresholds in section 0 and compute, in the script,
  \[
  d_M(t)=l_\star\left\|r(t)-(1-\mu_\star,0,0)\right\|.
  \]
  Locate its minimum using dense output/extremum search or boundary events, report its time and margin, and gate it explicitly. State whether 1900 km means **Moon-center distance** or altitude; they are different constraints. Likewise expose the positive-mass and all-burn mass-law checks rather than relying solely on one admissibility Boolean.

- **[ROBUSTNESS]** `DRO_tulip/indirect/transfer_study.m:350-357` -- **The study omits the malformed-data defenses explicitly required by the supporting certifier.** Residuals are not validated as real, finite, nonnegative scalars. For example, a negative “absolute residual” can satisfy `< tol`; NaN-omitting maxima can conceal one invalid gap input. NaN conjugate counters also make the `> 0` tests at lines 466–467 false, potentially preserving PASS. **Fix:** validate each input to a gate before arithmetic: residual domains, scalar logical flags, nonnegative integer counts, finite determinant/spectrum arrays, and valid time coverage. Fail closed on malformed data. The validation policy described in `certify_root.m:39-45` should be implemented here, not merely assumed of helper outputs.

- **[ROBUSTNESS]** `DRO_tulip/indirect/transfer_study.m:419-434` -- **The 10× lift margin measures only a limited sensitivity, not the total rank uncertainty.** The text appropriately acknowledges this, but the evidence still leaves open common errors shared by both integrations: trajectory/control interpolation, approximate-root error, constraint construction, and sampling. The acknowledged residual floor at lines 105–111 is precisely a reason that changing adjoint tolerance alone may not reveal the dominant contribution. **Fix:** display how `LM.errEst` is formed and test additional independent refinements, including trajectory/control and constraint-grid refinement. Compare \(\sigma_6\) with an error estimate in the same matrix norm that includes these contributions and explains the accepted-lift residual floor. Without such evidence, report “numerically rank-separated under this refinement,” not an established exclusion of abnormal lifts.

- **[ROBUSTNESS]** `DRO_tulip/indirect/transfer_study.m:327-335` -- **The finite-difference adjoint test has no enforced differentiation-accuracy criterion.** `PW.fdAgree` is printed, but only `PW.adjErr` determines N5 PASS. Richardson extrapolation does not itself show that the selected steps are in the truncation-dominated regime rather than cancellation or poor scaling. **Fix:** define and enforce an FD uncertainty threshold materially smaller than `tol.adj`, show a step-size sweep or equivalent error estimate, and identify the worst sample/component. If derivative uncertainty is too large, N5 is unresolved rather than PASS. An explicitly coded analytic/AD comparison is an additional useful check.

- **[CORRECTNESS]** `DRO_tulip/indirect/transfer_study.m:535-537` -- **The cross-check failure branch can print a false statement.** Reaching this branch establishes `necessary`, but does **not** establish `sufficient`. Thus “every PMP and sufficiency line passed” is false when a cross-check and a sufficiency gate both fail. **Fix:** say only that the PMP gates passed and the cross-check failed, then report the actual sufficiency status separately. Prefer listing all failed/unresolved gates rather than allowing branch precedence to misdescribe them.

### Ranked top-5

1. **Dense S4 FAIL can still reach the positive verdict** — lines 459–468.
2. **Jacobi quotient and free-time/free-mass reduction are not reproducible or independently gated** — lines 436–454.
3. **Library verdicts replace the required in-script optimality computations** — lines 278–285 and 364–381.
4. **Propagation uncertainty and cross-flight consistency are not established at the diagnostic margins** — lines 251–255, 298–303, and 497–499.
5. **Failed optimality checks still allow a successful script exit** — lines 514–552.