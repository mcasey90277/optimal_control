- **[CORRECTNESS]** `note:Route B / Theorem 1` — The losslessness theorem is stated for the exponential relaxation in Eqs. (9)–(11), while the implemented Route-B problem uses the conservative Taylor-restricted band in Eq. (12). G4 certifies only \(\lVert u\rVert=\sigma\), not equivalence of the Taylor program to the original problem. State explicitly that losslessness applies to the SOC slack under the theorem’s normality assumptions; the Taylor restriction remains a separately measured model error.
- **[CLARITY]** `note:Route B / Theorem 1 proof` — The proof sketch assumes inactive glideslope and then says path/bound multipliers “do not affect” the argument, without stating the full normality/constraint-qualification assumptions under which that remains true. Put those assumptions in the theorem statement, identify whether the flagship glideslope is inactive, and describe the proof as an intuition for the direction/tightness result rather than a proof of the implemented Taylor program.
- **[CLARITY]** `note:Certification / G5` — Eq. (25) supports midpoint alignment only after collecting all radial terms (mass-flow derivative and active annulus multipliers) and assuming no active pointing-cone constraint. State this directly; “forces exactly parallel” currently reads more generally than the displayed stationarity equation warrants.
- **[CORRECTNESS]** `note:Phase 2 / coast-extension mechanism` — “Coast-extension” and “free part of the deceleration” are inaccurate for a continuous-burn problem with \(T_{\min}>0\): the vehicle remains on a minimum-throttle burn and consumes propellant. Rename it “minimum-throttle-arc extension” and say that *drag’s incremental deceleration* is propellant-free.
- **[CORRECTNESS]** `note:Future work / pointing cone` — A finite pointing cone does not inherently force optimal thrust onto its boundary; this occurs only when the cone constraint is active. Replace the categorical wording with “when the cone is active, the constrained optimum may lie on its boundary…”.
- **[CLARITY]** `note:Parameters` — “Table 1 is the complete parameter set; every number follows from it plus the two solvers” is too broad: Monte-Carlo seed/dispersions, tracker weights, solver tolerances, and certification thresholds appear elsewhere. Scope the claim to the physical/PDG parameter set or cross-reference the other configuration tables.
- **[CONSISTENCY]** `SDD:Repository layout` — “Seventeen `.m` files ship” conflicts with the listed counts: \(1+1+8+2+4+11=27\). Either say “16 production modules plus 11 tests, 27 `.m` files total,” or revise the inventory/counts.
- **[CONSISTENCY]** `SDD:pdg_dynamics vs. Conventions` — Sections 3.3 and 8 disagree on the magnitude regularizer. The module description says MATLAB and CasADi both use `sqrt(sum(.^2)+1e-12)`; Conventions says MATLAB uses unregularized `sqrt(sum(.^2))` and only CasADi regularizes. This also conflicts with the note’s unqualified “\(\sqrt{\sum(\cdot)^2}\)” claim. Make all three descriptions reflect the shipped implementation and state the regularizer consistently.
- **[CONSISTENCY]** `SDD:Four adjudicated values / G3` — The SDD calls the measured Taylor-bound gap “0.70–0.94 kg,” whereas the note’s fixed-\(t_f\) refinement sweep reports 0.70–0.74 kg and the flagship result is 0.428 kg. Correct the range or identify its grid/time-search conditions and source so these are not competing descriptions of the same experiment.
- **[CLARITY]** `SDD:Certification / G3` — “\(L_\infty\) trajectory difference” is not a reproducible interface definition when Route A and B have different time grids and slightly different final times. Specify the state component(s), norm, interpolation method, comparison clock (physical time or normalized time), sampling grid, and whether terminal states are included.
- **[CORRECTNESS]** `SDD:Certification / pointing-cone primer mode` — Like the note, the explanation says that with a finite cone “the optimal thrust rides the cone boundary.” A finite cone can be inactive. Make the skip rationale conditional on an *active* cone constraint; retaining the conservative “unscored whenever finite” software policy is defensible, but it should not be justified by a false universal statement.
- **[STYLE]** `SDD:Extension points and known limitations` — Several historical narratives are valuable but repeated nearly verbatim in module entries, certification, test architecture, and limitations. Retain one authoritative explanation per lesson and cross-reference it; this will make the SDD easier to use as an engineering reference rather than a campaign ledger.

The **theory note** is unusually strong in traceability, numerical honesty, and explicit limitations; its main submission blocker is separating the exact lossless-convexification theorem from the implemented Taylor inner approximation and tightening a few categorical statements. The **SDD** is impressively detailed and largely extension-ready, but the file-count, regularizer, and G3-range contradictions must be fixed, and G3 needs a fully specified trajectory metric. **Verdict: revise before submitting the note**—the core results are credible, but the theorem/model-error boundary should be made mathematically unambiguous.
