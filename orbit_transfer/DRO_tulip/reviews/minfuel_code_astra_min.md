- **[CORRECTNESS]** `cr3bp_minfuel_pmp.m:127,141-143` — **Q1: Yes, generally.** Branchwise AD omits the sensitivity of the switching time. At a transverse crossing of \(h(y)=Q(y)-1=0\), apply
  \[
  \Phi^+=S\Phi^-,\qquad
  S=I_{14}+\frac{(F^+-F^-)\,n^\top}{n^\top F^-},\qquad
  n=\nabla Q=T_{\max}\begin{bmatrix}0_3\\0_3\\-\rho/m^2\\0_3\\\lambda_v/(m\rho)\\1/c\end{bmatrix},
  \quad \rho=\sqrt{\lambda_v^\top\lambda_v+10^{-300}}.
  \]
  Here \(F^-,F^+\) are the **incoming/outgoing one-sided fields**: throttle \(p\to1\) for an upward crossing, reversed for a downward crossing. **Fix:** locate each switch, stop integration, left-multiply the STM by \(S\), and restart on the outgoing branch. Grazing crossings (\(n^\top F^-=0\)) require separate treatment. The **`eps` family does not have this missing-jump defect** (`cr3bp_minfuel_pmp.m:119-121`): its field is continuous at the clipping boundaries, so \(F^+=F^-\) and \(S=I\); its piecewise Jacobian suffices at ordinary crossings.

- **[CORRECTNESS]** `ms_conjugate_test.m:117-118` — **Q2: Use \(\Phi(1:7,8:14)\) for the standard forward interior conjugate-point test; the mixed block is the terminal shooting derivative, not a generally valid interior focal-point exclusion.** The second variation has admissible boundary conditions \(\delta x(0)=0,\ \delta(r,v)(t_f)=0\), with \(\delta m(t_f)\) unrestricted. Its Jacobi kernel therefore satisfies the additional natural condition \(\delta\lambda_m(t_f)=0\): this justifies \(\Phi([1:6,14],8:14)\) **at \(t_f\)**. Interior fixed-state Jacobi fields instead satisfy \(\delta x(\tau)=0\), including \(\delta m(\tau)=0\), giving the full-state block. Imposing \(\delta\lambda_m(\tau)=0\) at an arbitrary interior time substitutes a different endpoint problem; its singularity need not refute minimality of the original problem. **Fix:** set `stateRows = 1:7` for this forward interior test and separately check the free-mass terminal second variation. Alternatively, implement a terminal-manifold focal test by propagating the terminal tangent space \(\{\delta(r,v)=0,\delta\lambda_m=0\}\) **backward from \(t_f\)** and testing its state projection. These statements require the usual regularity conditions for the Jacobi criterion.

- **[CORRECTNESS]** `ms_conjugate_test.m:132-136` — **Q2, endpoint exclusion: No.** Every sampled time belongs to `info.tGrid(2:K)`, strictly before \(t_f\); therefore the last sampled sign-change bracket is also interior. Subtracting `double(atFinal)` can incorrectly produce `pass = true`. **Fix:** use `atFinal = false; nIn = numel(flips) + nnz(dets == 0);` for the current sampling scheme. Reserve endpoint classification for a root actually located at \(t_f\), not merely the last sampled bracket.
