Here is the SCOPED FRESH REVIEW of the GTO-to-tulip direct pipeline, strictly audited against the lessons learned from the sibling campaign and specifically assessed for thrust-ladder readiness.

### 1. CONTINUATION AND ROBUSTNESS FINDINGS (L1–L6)

- **[METHODOLOGY]** `certify_minfuel_pmp.m:241` -- **L1: Switch count integer trap.** `cert.passed` demands a strict integer match (`matched == numel(swIdx)`). Bang-bang optimal control switch counts converge as bands across densities; demanding a static integer match without verifying via a doubled-mesh continuation causes spurious certification failures if a single switch aligns with a node edge. *Fix*: Relax to a fractional band (e.g., `matched >= numel(swIdx)*0.95`) or script an explicit mesh-halving convergence check before finalizing the count.
- **[ROBUSTNESS]** `minfuel_at_tf.m:84` (also `sundman_homotopy.m:47`) -- **L2: Under-iteration collapse blind spot.** The gating condition `ok = o.success && o.maxDefect < 1e-6` is permeable. Because IPOPT is given `acceptable_tol = 1e-5` (`casadi_minfuel_sundman.m:140`), CasADi emits `success=true` for an under-converged tail with a defect of `9e-7`. This legalizes garbage duals, poisoning the continuation sequence indefinitely. *Fix*: Harden the gate to `strcmp(o.ipoptStatus, 'Solve_Succeeded') && o.maxDefect < 1e-6`, completely blocking `"Solved_To_Acceptable_Level"` leakage.
- **[CORRECTNESS]** `casadi_minfuel_sundman.m:107` -- **L3: O(t_f)/N partial unscaled defect.** The dynamic defect `D` scales structurally with interval length $O(t_f/N) \approx 0.007$. Because it is passed unscaled, IPOPT’s `constr_viol_tol = 1e-7` acts on the *absolute* interval defect. This implies a loose $1.4 \times 10^{-5}$ relative interval error, permitting up to $\sim 4 \times 10^{-4}$ ND ($\approx 155$ km) of unmanaged global integration drift. *Fix*: Manually normal-scale the collocation constraint to standard derivative magnitude: `opti.subject_to((N/tauf) * D(:) == 0)`.
- **[CORRECTNESS]** `minfuel_at_tf.m:60` (and `energy_step.m:19`) -- **L4: Phase-aliasing global scaling.** `Xk(8,:) = Xk(8,:) * (tf / Xk(8,end))` applies a scalar stretch to the time mesh while leaving the spatial trajectories `X(1:6)` and controls `U` on their original mesh indices. Across a highly resonant spiral, even a moderate time-shift breaks dynamical phase coupling: the new target time maps older indices to non-optimal anomaly locations, sending "apogee thrust" to perigee. *Fix*: Resynchronize `X` and `U` node indices using `sundman_seed_map.m` (or interpolated mean-anomaly) at the new $t_f$ instead of a blind array-scalar multiplication.
- **[ROBUSTNESS]** `casadi_minfuel_sundman.m:114` -- **L5: Box saturation blind spot.** `[-12, 12]` velocity bounds map barely above the $~9.9$ ND GTO perigee limit. Should optimization push $v$ to `12.0`, IPOPT optimizes successfully *at the bound* returning `maxDefect ~ 0`. The codebase logs no warning, falsifying success. *Fix*: Widen to `[-25, 25]` and add a necessary output diagnostic: `out.boundSat = any(Xs(:)<=lbX(:)+1e-4 | Xs(:)>=ubX(:)-1e-4)`. 
- **[ROBUSTNESS]** `casadi_minfuel_sundman.m:103` -- **L6: Fixed continuation bound lock.** `tauf = tauf0;` statically freezes the regularized integration length dimension. Under a deep thrust ladder ($T_{max}$ sweeping off 25 mN), required gravity exposure changes, drastically shifting the true $\tau_f$ integral ($\int \kappa^{-1} dt$). A frozen scalar locks IPOPT out of warping the grid appropriately, creating structural infeasibility bounds. *Fix*: Unpin `tauf` allowing CasADi to stretch the boundary conditionally, or recalculate `tauf0` dynamically between every ladder step.
- **[EFFICIENCY]** `gen_tulip_energy_2p.m:181` -- **Triage Deferral Caught:** `step_solve` correctly kicks off a tight sweep `rT` if the loose probe `rL` works. However, if `rT` mysteriously falters, it straight returns `ok=false` instead of failing-over to `rF` (tight-from-Xk parameterization). *Fix*: Catch the failed `rT` and funnel it back through the `rF` initialization.

### 2. LADDER-READINESS ANALYSIS 

If you run a sweeping $T_{max}$ thrust ladder across this architecture right now, **it will fail almost immediately via structural mesh lock.**  
As $T_{max}$ drops, gravity losses force more revolutions; as it rises, it spirals faster. Holding `tauf = tauf0` frozen across rungs (L6) guarantees IPOPT cannot stretch the Sundman variable scope to hold different revolution profiles, breaking internal dynamics limits `ubX(8,:) = 2*tf`. Concurrently, the index-invariant warm-starting scale (L4) ensures that as timing morphs significantly, the prior guess's thrust profiles will completely phase-alienate the gravity well.  

**Top 3 requisite changes before the ladder sweep:**
1. **Dynamic $\tau_f$ update:** Pass an adaptively recalculated `tauf0 = sum(2*dt./(kapL+kapR))` before *every* solve instead of treating the original energy string's $\tau_f$ parameter as canonical.
2. **Phase-aware seed shifting:** Delete the scalar multiplication `Xk(8,:) = ...`. Force every parameter shift to execute a `sundman_seed_map()` re-alignment so the controls ($U$) align closely with their necessary radius-phase.
3. **Widen constraints & monitor binding:** Inflate `lbX` and `ubX` uniformly, adding `boundSat` monitoring logic to detect ceiling conditions during large $T_{max}$ shifts gracefully.

### 3. OVERALL VERDICT

**I TRUST the published numbers as scoped.** For a rigidly fixed physical configuration ($25$ mN, $15$ kg, near-nominal $t_f$), the KKT duals align and mesh nodes do not aggressively strain the hardcoded box limits logic. The solver core is mathematically sound inside its tiny local band.

**The single highest-value improvement:** Implement proper relative collocation scaling (`(N/tauf)*D(:) == 0`). Integrating raw defect strings under static error tolerances `1e-7` lets structural drift scale massively with $N$. Restoring standard scaling secures absolute dynamic certainty regardless of independent variable formulation.
