# attic — superseded code (kept for history, do not use)

Pre-Sundman-era direct solvers and prototypes, moved here 2026-07-09 by
`CODE_CLEANUP_PLAN.md` Phase 0. Everything here is superseded by
`../sundman_minfuel/` (see `../LOW_THRUST_MINFUEL_CAMPAIGN.md` for what each
generation taught). Notably: `casadi_minfuel_sundman.m` here is the STALE OLD
API (no warmTight) — it shadowed the library copy once; that is why it lives
here now. Untracked `.mat`/`.log` files are local-only campaign artifacts.
