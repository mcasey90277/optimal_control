- **[CORRECTNESS]** `orbit_transfer_exercises.tex:203-206, 540-547` and `primer_check.m:7-8` — the document’s primer sign convention is self-contradictory. With the stated Hamiltonian convention and `u^*=-\lambda_v`, the primer equal to the control is \(p=u^*=-\lambda_v\), so \(\dot p=-\dot\lambda_v=+\lambda_r\). The TeX gets this right at `orbit_transfer_exercises.tex:540-547`, but `primer_check.m:7-8` says “since pdot = lam_r” while the code comment block also writes `p(0)=-lam_v(0), pdot(0)=lam_r(0)` without explicitly tying it to `p=-lam_v`; meanwhile your review prompt’s “pdot = +lam_r” is the correct one. Concrete fix: in the tutorial, add one explicit line after `orbit_transfer_exercises.tex:203`: “We define the primer as \(p:=u^*=-\lambda_v\), hence \(\dot p=+\lambda_r\).” In `primer_check.m`, change the prose to “since \(p=-\lambda_v\), \(\dot p=-\dot\lambda_v=+\lambda_r\).”

- **[RUNNABILITY]** `orbit_transfer_exercises.tex:505-508` — Checkpoint D2 will not run as pasted in a fresh MATLAB session because `x0`, `xf`, `tf`, and `mu` are not defined in that snippet; they only exist in D1. Concrete fix: prepend the same setup line used in D1 (`mu = 1; x0 = ...; xf = ...; tf = 4.4;`) to D2.

- **[RUNNABILITY]** `orbit_transfer_exercises.tex:556-563` — Checkpoint E also depends on `x0`, `xf`, `tf`, and `mu` from earlier checkpoints, so it is not self-contained. Concrete fix: add the tutorial-instance definitions at the top of the snippet.

- **[AUDIENCE]** `orbit_transfer_exercises.tex:101-114` — the “Prerequisites” section says the companion theory note derives “everything used here,” which undercuts the later promise that “The astrodynamics you need” is sufficient for a student new to astrodynamics. As written, the astrodynamics primer is enough for the orbital facts, but not for the optimal-control derivations actually used. Concrete fix: say explicitly that no astrodynamics background is assumed, but prior exposure to PMP/costates is assumed unless the reader studies the companion note first.

- **[AUDIENCE]** `orbit_transfer_exercises.tex:129-133` — DU/TU are introduced abruptly as jargon. A math student can follow, but “Choose the distance unit DU … and the time unit TU …” assumes familiarity with unit-normalization language. Concrete fix: add one sentence: “DU and TU just mean ‘distance unit’ and ‘time unit’; we are rescaling variables so the departure radius and \(\mu\) become 1.”

- **[AUDIENCE]** `orbit_transfer_exercises.tex:143-150` — “semi-major axis” is used without definition, despite the promise that the astro section suffices. Concrete fix: define it inline: “for an ellipse, the semi-major axis \(a\) is half the long diameter; for the Hohmann ellipse here \(a=(r_0+r_f)/2\).”

- **[AUDIENCE]** `orbit_transfer_exercises.tex:167-170, 609-614` — “rendezvous,” “manifold,” and “transversality” are used as if already familiar. They are standard optimal-control terms, not astrodynamics terms, but for the stated audience they still need one-line glosses. Concrete fix: add brief parentheticals where first used, e.g. “rendezvous = match the entire state at a prescribed point,” “manifold = a set of acceptable terminal states,” “transversality = the extra endpoint condition that replaces the missing fixed-endpoint equations.”

- **[PEDAGOGY]** `orbit_transfer_exercises.tex:104-108, 280-288, 526-543` — several central formulas are imported from the companion note rather than re-motivated locally: the costate ODEs, the \(2n\) boundary-count rule, and especially why the primer obeys \(p''=Gp\). A strong math student can implement from formulas, but may not understand why these are true from this document alone. Concrete fix: add 1–2 derivation lines at each first use: e.g. after `orbit_transfer_exercises.tex:280`, show \(\dot\lambda_v=-\partial H/\partial v=-\lambda_r\); after `orbit_transfer_exercises.tex:526`, show \(p=-\lambda_v\Rightarrow \ddot p=-\ddot\lambda_v=G(-\lambda_v)=Gp\).

- **[PEDAGOGY]** `orbit_transfer_exercises.tex:463-479` — the collocation hint is excellent, but it effectively gives away the full construction of the initial guess while the exercise text only says “see the Hint before writing this.” That is more “solve” than “rescue.” Concrete fix: move the polar-spiral idea into the exercise and keep the hint narrower: “If your first guess makes the constraint undefined, inspect whether any guessed radius hits zero.”

- **[PEDAGOGY]** `orbit_transfer_exercises.tex:413-418` — “If it converges to a different \(\lambda_0\) with the same \(\|\psi\|\approx0\) and a larger \(J\): you found a different extremal” is mathematically true, but for a beginner it is too advanced and not very actionable. Concrete fix: replace with “this likely indicates a different stationary solution; first verify your signs and restart from the provided zero guess before concluding multiple extremals exist.”

- **[CORRECTNESS]** `orbit_transfer_exercises.tex:489-500` — the exitflag explanation is defensible for this code/problem. `collocation_transfer.m:68-79` uses `fmincon` interior-point with tight tolerances, and a feasible point with tiny defect and `exitflag = 2` is indeed acceptable. Concrete fix: none required mathematically; if you want more precision, say “acceptable for this problem because feasibility and optimality residuals are already at tolerance.”

- **[CORRECTNESS]** `orbit_transfer_exercises.tex:493-499, 517-520` — the claim “\(J_c < J_{ind}\) because the discretized problem is slightly cheaper” is broadly defensible here, and the observed gap scales by about 4 under \(N\mapsto 2N\), matching second-order trapezoidal transcription. But strictly speaking the table measures convergence of the discrete optimum’s objective to the continuous one for this instance, not a theorem that the discrete NLP is always cheaper. Concrete fix: soften to “for this instance the mesh-relaxed NLP is slightly cheaper than the continuous optimum; the gap shrinks empirically like \(O(h^2)\).”

- **[CORRECTNESS]** `orbit_transfer_exercises.tex:146-154` — the Hohmann time computation is correct: \(a=(1+1.5)/2=1.25\), \(t_H=\pi\sqrt{1.25^3}=4.390509...\). The circular speeds at `orbit_transfer_exercises.tex:141-142` and the target-state sign at `160-166` also match the code and hand calculation. Concrete fix: none.

- **[CORRECTNESS]** `orbit_transfer_exercises.tex:283-287, 235-237, 428-433, 345-349` — the stated dynamics, gravity-gradient matrix, trapezoidal defect definition, and the fixed-endpoint \(2n\) counting all match the MATLAB implementation (`ocp_dynamics.m:24-36`, `gravity_gradient.m:19-20`, `collocation_transfer.m:88-99`, `shoot_residual.m:4-7`). Concrete fix: none.

- **[RUNNABILITY]** `orbit_transfer_exercises.tex:558-560` — the checkpoint snippet does run with the actual function signatures: MATLAB permits dropping trailing outputs, so `[lam0, sol] = solve_indirect(...)` and `[Xc, Uc, tc] = collocation_transfer(...)` are valid against `solve_indirect.m:1` and `collocation_transfer.m:1`. Concrete fix: none.

- **[RUNNABILITY]** `orbit_transfer_exercises.tex:265-272, 327-329, 369-373, 405-412, 487-492, 510-516, 565-570` — the expected numbers are consistent with the reference code and direct re-derivation: the gravity-gradient matrix, Hamiltonian value, coast miss, Hohmann time, circular speeds, and the D2 factor-of-4 staircase all check out against `verify_checkpoints.m`. Concrete fix: none.

- **[STYLE]** `orbit_transfer_exercises.tex:104-108, 633-635` — the document references `myLatex/notes/cov_pmp_orbit_transfer.tex`, but that file is not present in this directory. The local reference implementation is enough to work through the tutorial, so this dangling external dependency is structurally awkward. Concrete fix: either bundle that note, or rewrite as “companion note (not included in this folder).”

- **[STYLE]** `orbit_transfer_exercises.tex:588-595` — figure inclusion is structurally fine; `expected_result.png` exists. No broken internal refs were found (`sec:astro`, `fig:expected`, `sec:next` all resolve syntactically). Concrete fix: none.

- **[STYLE]** build environment — I could not verify PDF compilation because `pdflatex` is not installed in this environment. I found no obvious LaTeX syntax break by inspection, but this remains uncompiled here. Concrete fix: run `pdflatex orbit_transfer_exercises.tex` in an environment with TeX installed and check for overfull boxes around the wide verbatim checkpoints.
